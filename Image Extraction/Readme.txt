The first file is the Ipython file for filtering the images in the mscoco 
dataset.

Next is subset_train folder which contains a small subset of images
which are taken as an input by the "preprocess.py" file 

Libraries used in preprocess.py file
import OS
From os.path import join
import numpy
import CV2
These are the libraries we are using please ensure to have these 
downloaded otherwise the file "preprocess.py" will not run

I DEVELOPED THE CODE FOR PREPROCESS.PY WITH PYTHON 3.8.3
IN ANACONDA ENVIRONMENT. THE EDITOR USED WAS 
ANACONDA SPYDER